# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2024"
rust-version = "1.94"
name = "ktir-emulator"
version = "0.1.0"
build = "build.rs"
exclude = ["tests/fixtures/**"]
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "KTIR execution layer — the Spyre emulator: ktdp interpreter, machine-state model, and Metal/NAX/AMX accelerator backend (RFC 0682)."
readme = false
license = "Apache-2.0"
repository = "https://github.com/torch-spyre/ktir-cpu"

[package.metadata.docs.rs]
targets = ["aarch64-apple-darwin"]
default-target = "aarch64-apple-darwin"

[features]
# ⛔ `src/blas.rs` and `src/lib.rs` used to select the Accelerate `cblas_sgemm` backend on
# `cfg(target_os = "macos")`, which made `cblas-sys` and `blas-src` NON-OPTIONAL for every macOS
# build — so a consumer with no crates.io access could not resolve this crate at all. This feature
# is that switch, moved off the target; the default build now resolves with no registry package.
# ⚖️ Default it on for `cfg(target_os = "macos")` in the workspace to restore the old behaviour
# exactly. Turning it on IS the old macOS behaviour, byte for byte.
accelerate = [
    "dep:cblas-sys",
    "dep:blas-src",
    "blas-src/accelerate",
]
blis = [
    "dep:cblas-sys",
    "dep:blas-src",
    "blas-src/blis",
]
default = ["optimizer"]
metal = [
    "dep:objc2",
    "dep:objc2-metal",
    "dep:objc2-foundation",
]
mkl = [
    "dep:cblas-sys",
    "dep:blas-src",
    "blas-src/intel-mkl-dynamic-sequential",
]
openblas = [
    "dep:cblas-sys",
    "dep:blas-src",
    "blas-src/openblas",
]
openblas-system = [
    "openblas",
    "dep:openblas-src",
    "openblas-src/system",
]
optimizer = ["dep:ktir-optimizer"]

[lib]
name = "ktir_emulator"
path = "src/lib.rs"

[[example]]
name = "bench"
path = "examples/bench.rs"

[[example]]
name = "ktir_diff_run"
path = "examples/ktir_diff_run.rs"

[[example]]
name = "mtl4_smoke"
path = "examples/mtl4_smoke.rs"
# ⛔ This example `use`s `ktir_emulator::metal` unconditionally, and that module is `cfg(metal)`.
# It used to build on macOS because build.rs set `cfg(metal)` from the TARGET; now that the
# backend is feature-selected, say so here instead of letting the example fail to compile.
required-features = ["metal"]

[[test]]
name = "bench_amx_vs_metal"
path = "tests/bench_amx_vs_metal.rs"

[[test]]
name = "flash_attn_timing"
path = "tests/flash_attn_timing.rs"

[[test]]
name = "gpu_dispatch_floor"
path = "tests/gpu_dispatch_floor.rs"

[[test]]
name = "metal_conformance"
path = "tests/metal_conformance.rs"

[[test]]
name = "port_dialects_exec"
path = "tests/port_dialects_exec.rs"

[[test]]
name = "port_dtypes"
path = "tests/port_dtypes.rs"

[[test]]
name = "port_indirect_access"
path = "tests/port_indirect_access.rs"

[[test]]
name = "port_lx_scoping"
path = "tests/port_lx_scoping.rs"

[[test]]
name = "port_ops"
path = "tests/port_ops.rs"

[[bench]]
name = "smollm2_bench"
path = "benches/smollm2_bench.rs"
harness = false

[dependencies.blas-src]
workspace = true
optional = true

[dependencies.cblas-sys]
version = "0.1"
optional = true

[dependencies.ktir-core]
version = "0.1.0"
path = "../ktir-core"

[dependencies.ktir-optimizer]
version = "0.1.0"
path = "../ktir-optimizer"
optional = true

[dependencies.objc2]
version = "0.6"
optional = true

[dependencies.objc2-foundation]
version = "0.3"
optional = true

[dependencies.objc2-metal]
version = "0.3"
optional = true
features = [
    "MTL4ArgumentTable",
    "MTL4CommandAllocator",
    "MTL4CommandBuffer",
    "MTL4CommandEncoder",
    "MTL4CommandQueue",
    "MTL4ComputeCommandEncoder",
    "MTL4ComputePipeline",
    "MTLAllocation",
    "MTLResidencySet",
]

[dependencies.openblas-src]
version = "0.10"
optional = true
default-features = false

[dev-dependencies]
serde_json = "1"
